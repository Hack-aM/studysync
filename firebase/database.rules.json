{
  "rules": {
    "presence": {
      "groups": {
        "$groupId": {
          ".read": "auth != null",
          "$uid": {
            ".write": "auth != null && auth.uid === $uid"
          }
        }
      }
    }
  }
}
